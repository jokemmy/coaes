
export default {
  root: process.cwd(),
  analyze: true,
  basename: '/',
  hash: '',
  history: 'browser',
  manifest: '',
  build: 'dist',
  plugins: [],
  webpack: ( config ) => config
  // indexTemplate: '',
  // mountElementId: 'root'
};
